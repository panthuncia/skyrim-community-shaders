# Community Shaders uses an HDR10 (RGB10A2 + PQ/BT.2020) swapchain when its
# HDR Display feature is enabled. DXVK otherwise hides HDR10 colorspaces even
# when Windows HDR is active, causing PQ output to be presented as SDR.
dxgi.enableHDR = True
